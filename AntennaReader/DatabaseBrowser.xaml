<Window x:Class="AntennaReader.DatabaseBrowser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Diagram Manager"
        Height="500" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="SearchBar"
                     Style="{StaticResource StandardInput}"
                     Height="28"
                     Padding="6,0,6,0"
                     Margin="0"
                     TextChanged="SearchBar_TextChanged"/>

            <TextBlock Text="Search (e.g. Antenna Name / State / City )"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource HintText}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBar}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <ListView x:Name="DiagramList"
                  Grid.Row="1"
                  Margin="0,0,0,10"
                  SelectionMode="Extended">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Edit Antenna Metadata" Click="Edit_Click"/>
                    <MenuItem Header="Import to Diagram" Click="Import_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50"
                                    DisplayMemberBinding="{Binding Id}" />

                    <GridViewColumn Header="Antenna Name" Width="150"
                                    DisplayMemberBinding="{Binding AntennaName}" />

                    <GridViewColumn Header="Antenna Owner" Width="150"
                                    DisplayMemberBinding="{Binding AntennaOwner}" />

                    <GridViewColumn Header="State" Width="100"
                                    DisplayMemberBinding="{Binding State}" />

                    <GridViewColumn Header="City" Width="120"
                                    DisplayMemberBinding="{Binding City}" />

                    <GridViewColumn Header="Created" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CreateDate}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Import to diagram" Style="{StaticResource StandardBtn}" Click="Import_Click"/>
            <Button Content="Export CSV" Style="{StaticResource StandardBtn}" Click="ExportCSV_Click"/>
            <Button Content="Export PAT" Style="{StaticResource StandardBtn}" Click="ExportPAT_Click"/>
            <Button Content="Clear Selection" Style="{StaticResource StandardBtn}" Click="ClearSelection_Click"/>
            <Button Content="Delete Antenna(s)" Style="{StaticResource DangerBtn}" Click="Delete_Click"/>
        </StackPanel>

    </Grid>
</Window>